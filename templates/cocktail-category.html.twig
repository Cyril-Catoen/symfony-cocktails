{% extends 'base.html.twig' %}

    {% block metatitle %}
    <!-- Nomme l'onglet du navigateur -->
    <title>Category Cocktails</title>
    {% endblock metatitle %}

	{% block main %}
		<main>
			<h1>Liste des catégories :</h1>
            {% for category in categories %}

            <article class="list-articles article">
                <h2>{{ category.nom }}</h2>
                <a href="{{ path('selected-category', { 'id': category.id }) }}">Plus d'informations - cliquez ici</a>	
            </article>
    
        {% endfor %}
		</main>
	{% endblock main %}